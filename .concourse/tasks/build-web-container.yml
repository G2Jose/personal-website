---
platform: linux

image_resource:
  type: docker-image
  source: {repository: docker}

inputs:
- name: resource-personal_website

outputs:
- name: web-docker-image

run:
  # path: /bin/sh resource-personal_website/.concourse/tasks/build-web-container.sh
  path: ls
  args: [-lap]
